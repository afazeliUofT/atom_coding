# Frozen adjudication protocol

## What commit `29e18ba` established

The v5 standard campaign produced all 9,120 planned compiled rows. The executable returned code 3 because seven `n=64, R=0.75` rows were not jointly certified under the frozen resource caps. All seven had survivor-substitution weight `E=3`; there was no completed-decoder tie-set mismatch.

## Why a single replay is scientifically legitimate

The v5 history cap was 2,000,000. For one deletion at `n=64`, complete probability shells through weight three contain

\[
2n\sum_{i=0}^{3}\binom{n-1}{i}=5,341,184
\]

histories. At `p=0.005` and `p=0.01`, the deterministic certificate shell offset is zero, so complete processing through shell three is the theorem-derived worst-case cap for an `E=3` transmitted history. The replay cap is therefore fixed by the theorem, not fitted to observed runtime.

The exact prefix cap is increased from 1,000,000 to 10,000,000 nodes solely to complete the independent reference on the same seven observations.

## Non-retroactivity

Commit `29e18ba` remains a failed 2M-cap closure. This R1 adjudication may authorize a manuscript only with the original cap failures disclosed. It cannot erase or overwrite the negative tail evidence.

## Terminal rule

This is the final performance adjudication. A negative or narrow result ends performance escalation. No additional cap increase, new baseline selection, or changed statistical threshold is authorized.
