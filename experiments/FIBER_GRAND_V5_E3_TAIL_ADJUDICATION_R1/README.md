# FIBER-GRAND v5 E=3 Tail Adjudication R1

This is a one-time, non-retroactive adjudication of commit `29e18ba`.

The original v5 campaign completed 9,120 compiled trials but returned process code 3 because seven `n=64, R=0.75` rows did not certify under the frozen caps. All seven were survivor-substitution weight `E=3`. Four had a certified prefix-A* reference; three exhausted both the 2,000,000-history FIBER cap and the 1,000,000-node prefix cap. No completed pair showed a tie-set disagreement.

For one deletion at `n=64`, all histories through shell three number

`2*64*sum_{i=0}^3 C(63,i) = 5,341,184`.

This package deterministically replays exactly those seven rows using that theorem-derived shell-complete FIBER cap and a 10,000,000-node exact prefix cap. It then patches an analysis copy—not the original data—recomputes the frozen v5 statistical contract, and produces one final classification.

Possible classifications:

- `AUTHORIZE_FULL_TIT_MANUSCRIPT_AND_EXTERNAL_REVIEW`
- `NARROW_TO_FIXED_EDIT_TIT_THEORY_ALGORITHM_PAPER`
- `STOP_FLAGSHIP_PERFORMANCE_CLAIM`

No further performance-rescue gate is authorized after this adjudication.
