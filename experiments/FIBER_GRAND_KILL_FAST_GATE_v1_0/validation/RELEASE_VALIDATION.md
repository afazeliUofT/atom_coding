# FIBER-GRAND Kill-Fast Gate v1.0 - Release Validation

## Scope

This release is an early scientific investment gate, not a universal impossibility proof and not a final novelty opinion. The authoritative profile is `standard`.

## Reference execution

- All 14 unit tests passed.
- All six higher-level exactness/audit groups passed.
- E0-E6 completed without a scientific or environment error.
- Reference classification: `PIVOT_TO_REVISED_FIBER_TRACK`.
- Authoritative command: `STOP_ORIGINAL_FIBER_GRAND_CLAIM_SET`.

## Interpretation

The reference run supports a finite-block computational mechanism for certified aggregate inverse search in the tested one-deletion and one-insertion settings, and gives a positive abstract-work signal for two deletions. It does not close the novelty, strong-baseline, asymptotic-theorem, or calibrated-use-case gates.

The revised path must not claim that `fiber guesswork` is a new generic random variable: under a uniform ambient input prior it is posterior conditional guesswork. The next contract must confront priority-first/A*, exact channel-code trellises, and specialized synchronization decoders.

## PDF validation

The theoretical dossier was compiled twice, preflighted, inspected, rendered at 160 dpi, and visually reviewed. The representative pages are retained under `validation/pdf_validation/representative_pages/`.

## Reproducibility

The user launcher verifies the outer ZIP SHA-256 and the internal package manifest, performs safe extraction, creates an isolated virtual environment, runs unit tests before the scientific gate, writes machine-readable results and command files, and stages only this experiment directory for Git publication.
