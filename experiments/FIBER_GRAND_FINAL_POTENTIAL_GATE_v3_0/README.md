# FIBER-GRAND Final Potential Gate v3.0

This package is the final **early-investment** gate for the narrowed fixed-edit FIBER-GRAND track. It does not restore the withdrawn broad claims.

## What it tests

1. exact codebook-free aggregate inverse decoding and strict certificates;
2. a primary-source novelty matrix for the narrowed theorem-and-algorithm synthesis;
3. exact finite-length audits of new moment, tail, and large-deviation results;
4. the predicted typical-versus-mean rate-complexity phase diagram;
5. FIBER versus two code-specific exact searches near the predicted phase boundary;
6. a true linear-time single-deletion VT decoder as an honest specialization boundary;
7. a calibrated synthetic post-front-end timing-slip model plus optional measured CSV traces.

## Run inside the installed package

```bash
python -m pytest -q tests
python -m fiber_final.run_gate --profile standard
```

The external launcher performs safe installation, creates the venv, runs tests and the gate, and commits only this experiment directory.

## Reference validation

The frozen internal standard run completed 18/18 tests and returned:

```text
CONTINUE_HIGH_POTENTIAL_FIXED_EDIT_FIBER_TRACK_REAL_TRACE_REQUIRED
FIELD_DEFINING_STATUS=HIGH_POTENTIAL_NOT_ESTABLISHED
```

This is a high-potential continuation signal for the narrow fixed-edit track, not a field-defining verdict.  The theoretical dossier is `theory/FIBER_GRAND_Final_Novelty_Theory_and_Potential_Gate.pdf`.
